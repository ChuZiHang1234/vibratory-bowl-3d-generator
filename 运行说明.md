# 振动盘3D生成器运行说明

## 直接运行

打开交付包中的：

```text
app/振动盘3D生成器.exe
```

注意：`app` 文件夹里的 `.dll`、`.pak`、`resources` 等文件需要和 exe 保持在同一个目录内，不要只单独复制 exe。

## 源码开发运行

首次开发需要安装依赖：

```bash
npm.cmd install
```

本地预览：

```bash
npm.cmd run dev
```

构建：

```bash
npm.cmd run build
```

桌面窗口预览：

```bash
npm.cmd run desktop:dev
```

