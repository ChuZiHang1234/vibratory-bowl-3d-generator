# 振动盘3D生成器运行说明

## 安装运行

打开交付包中的安装程序：

```text
installer/振动盘3D生成器-安装程序-0.1.0.exe
```

安装过程中会弹出提示，询问是否需要在桌面创建快捷方式。

软件运行后可在右上角选择导出格式，目前支持 STL、OBJ、STEP。出料姿态可在右侧参数面板中选择，例如正面朝上、背面朝上、侧面朝上或立式出料。

## 免安装运行

如使用免安装版本，打开：

```text
app/振动盘3D生成器.exe
```

注意：免安装版本中 `app` 文件夹里的 `.dll`、`.pak`、`resources` 等文件需要和 exe 保持在同一个目录内，不要只单独复制 exe。

## 源码开发运行

首次开发需要安装依赖：

```bash
npm.cmd install
```

本地预览：

```bash
npm.cmd run dev
```

构建：

```bash
npm.cmd run build
```

打包安装程序：

```bash
npm.cmd run package:win
```

打包免安装版本：

```bash
npm.cmd run package:portable
```

桌面窗口预览：

```bash
npm.cmd run desktop:dev
```
